# Dependencies (regenerated with yarn / composer)
node_modules
vendor
.yarn/*
!.yarn/patches
!.yarn/releases

# Build artifacts (regenerated with `yarn build` / `yarn package`)
build
dist

# Tooling caches
.phpunit.cache
.phpunit.result.cache

# Local env / OS noise
.env
.env.*
*.log
.DS_Store
